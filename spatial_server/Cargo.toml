[package]
name = "spatial_server"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
tokio = { version = "1.30", features = ["full"] }
bevy = { version = "0.18.1", default-features = false }
