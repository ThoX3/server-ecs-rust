[workspace]
resolver = "2"
members = [
    "shared",
    "dedicated_server",
    "orchestrator",
    "gatekeeper",
    "game_sockets",
    "broker",
    "spatial_server",
    "client"
]

[profile.release]
opt-level = 3
lto = "thin"
